interface:
  display_name: "HVL Git Exploration"
  short_description: "按需研究分级、假设验证与 Git 回退探索系统"
  default_prompt: "Use $hvl-git-exploration for this exploratory task: classify research need, scout prior art only if needed, form hypotheses, validate with evidence, and maintain Git checkpoints."
policy:
  allow_implicit_invocation: true
