interface:
  display_name: "HVL Git Exploration"
  short_description: "结合前人研究、假设验证与 Git 回退的探索系统"
  default_prompt: "Use $hvl-git-exploration for this research-like task: scout prior art, form hypotheses, validate with evidence, and maintain Git checkpoints."
policy:
  allow_implicit_invocation: true
